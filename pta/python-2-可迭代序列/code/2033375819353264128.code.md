```python3
from random import randint,seed
x,n = map(int,input().split())
seed(x)
cnt = 0
for i in range(n):
    l = [randint(1,365) for i in range(23)]
    if(len(set(l)) == 23):
        continue
    cnt += 1

rate = cnt / n
print(f"{rate=:.2f}")
```
