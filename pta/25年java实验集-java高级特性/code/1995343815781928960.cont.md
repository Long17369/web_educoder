# 统计字符串中字符的个数
输入一个字符串，分别统计字符串中出现的大写字母、小写字母个数，以及出现的数字字符以及其他字符的个数。

### 输入格式:

输入一个字符串。

### 输出格式:

输出字符串中大写字母、小写字母个数，以及出现的数字字符以及其他字符的个数。

### 输入样例:

在这里给出一组输入。例如：

```in
aBc3#d?>2D
```

### 输出样例:

在这里给出相应的输出。例如：

```out
小写字母有: 3个
大写字母有: 2个
数字有: 2个
其他字符有: 3个
```
