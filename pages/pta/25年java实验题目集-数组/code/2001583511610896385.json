{
  "title": "不等长二维数组",
  "description": "# 不等长二维数组\n二维数组有多个一维数组构成（不一定长度相等）。\n求出二维数组的最小值min。\n将二维数组的每个元素的值修改为原来的值减去min。并输出整个二维数组。 \n\n### 输入格式:\n\n先输入一个整数row，表示二维数组有多少个一维数组构成。\n然后输入row行。每行先输入一个整数column，表示一维数组的长度。然后输入column个整数，为此一维数组的元素。\n由上面row个一维数组，构成一个二维数组。 \n\n### 输出格式:\n\n求出二维数组的最小值min。\n将二维数组的每个元素的值修改为原来的值减去min。\n输出整个二维数组，构成二维数组的每个一维数组单独一行，每两个元素用空格分开。 \n\n### 输入样例:\n\n```in\n3\n4 2 3 4 5\n3 3 2 1\n2 6 7\n```\n\n### 输出样例:\n\n```out\n1 2 3 4\n2 1 0\n5 6\n```\n\n",
  "files": [
    {
      "language": "java",
      "code": "import java.util.Scanner;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int row = sc.nextInt();\n        int min_num = Integer.MAX_VALUE;\n        int[][] nums = new int[row][];\n        for (int i = 0; i < row; i++) {\n            int col = sc.nextInt();\n            nums[i] = new int[col];\n            for (int j = 0; j < col; j++) {\n                nums[i][j] = sc.nextInt();\n                min_num = Math.min(min_num, nums[i][j]);\n            }\n        }\n        for (int[] is : nums) {\n            for (int i = 0; i < is.length; i++) {\n                is[i] -= min_num;\n                System.out.print(is[i]);\n                if (i != is.length - 1) {\n                    System.out.print(\" \");\n                } else {\n                    System.out.println();\n                }\n            }\n        }\n        sc.close();\n    }\n}"
    }
  ]
}